const canvas=document.getElementById('bg-canvas');const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,.1,1000);const renderer=new THREE.WebGLRenderer({canvas,alpha:!0,antialias:!0});renderer.setSize(window.innerWidth,window.innerHeight);renderer.setPixelRatio(window.devicePixelRatio);camera.position.z=30;const particlesGeometry=new THREE.BufferGeometry();const particlesCount=3000;const posArray=new Float32Array(particlesCount*3);for(let i=0;i<particlesCount*3;i++){posArray[i]=(Math.random()-.5)*100}particlesGeometry.setAttribute('position',new THREE.BufferAttribute(posArray,3));const particlesMaterial=new THREE.PointsMaterial({size:.15,color:0x3b82f6,transparent:!0,opacity:.8,blending:THREE.AdditiveBlending});const particlesMesh=new THREE.Points(particlesGeometry,particlesMaterial);scene.add(particlesMesh);const bottles=[];const bottleGeometries=[new THREE.CylinderGeometry(.5,.7,2,6),new THREE.BoxGeometry(1,2,1),new THREE.ConeGeometry(.6,2,6),new THREE.OctahedronGeometry(1)];for(let i=0;i<15;i++){const geometry=bottleGeometries[Math.floor(Math.random()*bottleGeometries.length)];const material=new THREE.MeshPhongMaterial({color:Math.random()>.5?0x1e3a8a:0x3b82f6,transparent:!0,opacity:.3,shininess:100,wireframe:Math.random()>.5});const bottle=new THREE.Mesh(geometry,material);bottle.position.x=(Math.random()-.5)*50;bottle.position.y=(Math.random()-.5)*50;bottle.position.z=(Math.random()-.5)*50;bottle.rotation.x=Math.random()*Math.PI;bottle.rotation.y=Math.random()*Math.PI;bottle.userData={rotationSpeed:{x:(Math.random()-.5)*.02,y:(Math.random()-.5)*.02,z:(Math.random()-.5)*.02},floatSpeed:Math.random()*.02+.01,floatOffset:Math.random()*Math.PI*2};bottles.push(bottle);scene.add(bottle)}const ambientLight=new THREE.AmbientLight(0xffffff,.5);scene.add(ambientLight);const pointLight1=new THREE.PointLight(0x3b82f6,2);pointLight1.position.set(10,10,10);scene.add(pointLight1);const pointLight2=new THREE.PointLight(0x1e3a8a,2);pointLight2.position.set(-10,-10,10);scene.add(pointLight2);let mouseX=0;let mouseY=0;document.addEventListener('mousemove',(event)=>{mouseX=(event.clientX/window.innerWidth)*2-1;mouseY=-(event.clientY/window.innerHeight)*2+1});let time=0;function animate(){requestAnimationFrame(animate);time+=.01;particlesMesh.rotation.y+=.001;particlesMesh.rotation.x+=.0005;bottles.forEach((bottle,index)=>{bottle.rotation.x+=bottle.userData.rotationSpeed.x;bottle.rotation.y+=bottle.userData.rotationSpeed.y;bottle.rotation.z+=bottle.userData.rotationSpeed.z;bottle.position.y+=Math.sin(time+bottle.userData.floatOffset)*bottle.userData.floatSpeed});pointLight1.position.x=Math.sin(time)*15;pointLight1.position.z=Math.cos(time)*15;pointLight2.position.x=Math.cos(time*.7)*15;pointLight2.position.z=Math.sin(time*.7)*15;camera.position.x+=(mouseX*5-camera.position.x)*.05;camera.position.y+=(mouseY*5-camera.position.y)*.05;camera.lookAt(scene.position);renderer.render(scene,camera)}animate();window.addEventListener('resize',()=>{camera.aspect=window.innerWidth/window.innerHeight;camera.updateProjectionMatrix();renderer.setSize(window.innerWidth,window.innerHeight)});let cartCount=0;const cartCountElement=document.querySelector('.cart-count');const addToCartButtons=document.querySelectorAll('.add-to-cart');addToCartButtons.forEach(button=>{button.addEventListener('click',(e)=>{cartCount++;cartCountElement.textContent=cartCount;cartCountElement.style.transform='scale(1.5)';setTimeout(()=>{cartCountElement.style.transform='scale(1)'},200);const originalText=button.textContent;button.textContent='✓ تمت الإضافة';button.style.background='#4CAF50';setTimeout(()=>{button.textContent=originalText;button.style.background=''},1500)})});document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();const target=document.querySelector(this.getAttribute('href'));if(target){target.scrollIntoView({behavior:'smooth',block:'start'})}})});const observerOptions={threshold:.1,rootMargin:'0px 0px -100px 0px'};const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.style.opacity='1';entry.target.style.transform='translateY(0)'}})},observerOptions);document.querySelectorAll('.product-card').forEach(card=>{card.style.opacity='0';card.style.transform='translateY(50px)';card.style.transition='all 0.6s ease';observer.observe(card)});window.addEventListener('scroll',()=>{const scrolled=window.pageYOffset;const hero=document.querySelector('.hero-content');if(hero){hero.style.transform=`translateY(${scrolled*.5}px)`;hero.style.opacity=1-scrolled/500}});
